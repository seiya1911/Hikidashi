<div class='container px-5 px-sm-0'>
  <div class='row'>

    <div class='col-md-12 text-center'>
      <h2 class="mt-3">タグ一覧(Total<%= @tags.count %>)</h2>
      <div class="mt-5">
        <%= render 'index' %>
      </div>
    </div>

    <div class="col-md-12 text-center">
        <%= link_to '戻る', :back, class: 'btn btn-primary' %>
    </div>

  </div>
</div>