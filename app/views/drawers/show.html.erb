<div class="container">
 <h2 class="head-line title_h2 text-center">Drawer詳細</h2>
 <div calss="row">
    <div class="col-xs-8">
      <strong><i class="far fa-user"><% @drawer.name %></i></strong>
    </div>
    <div class="col-xs-3 col-xs-offset-1 billing">
      <strong><i class="fas fa-store">
        <table class="table table-striped table-bordered table-condensed">
          <thead>
            <tr>
              <th>説明</th>
              <td><%= @drawer.description %></td>
            </tr>
            <tr>
              <th>例</th>
              <td></td>
            </tr>
            <tr>
              <th>例</th>
              <td></td>
            </tr>
        </table>
    </div>
    <div class="col-xs-7 order-details">
      <strong><i class="fas fa-store"></i>Item一覧</strong>
        <table class="table table-striped table-bordered table-condensed">
          <thead>
            <tr>
              <th>Item名</th>
              <th>説明</th>
              <th>画像</th>
            </tr>
          </thead>
          <tbody>
              <% @items.each do |item| %>
            <tr>
              <td><%= item.name %></td>
              <td><%= item.description %></td>
              <td><%= attachment_image_tag item, :image, fallback:"no-image.jpeg", size: '250x200' %>></td>
            </tr>
              <% end %>
          </tbody>
        </table>
    </div>
  </div>
</div>
